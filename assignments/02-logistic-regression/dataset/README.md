# Dataset

This assignment uses the **Breast Cancer Wisconsin Dataset** provided by Scikit-Learn.

The dataset is loaded directly within the notebook using:

```python
from sklearn.datasets import load_breast_cancer

data = load_breast_cancer()
```

Since the dataset is bundled with Scikit-Learn, no external dataset file is required in this repository.